[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    slow: 标记运行时间较长的测试 (如大规模性能测试)
    integration: 集成测试
    unit: 单元测试
